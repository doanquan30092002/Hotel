# ─── Next.js (apps/web) ──────────────────────────────────────────────────────
# URL gốc của Backend API. Production: trỏ vào Railway domain.
# Lưu ý: phải có hậu tố /api/v1 (khớp API_PREFIX của BE).
NEXT_PUBLIC_API_BASE_URL="http://localhost:3001/api/v1"
